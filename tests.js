QUnit.test( "hello test", function( assert ) {
  assert.ok( 1 == "1", "Passed!" );
});

// given the environment I'm using includes "jamie"
// when I pass in "jamie" as a flag
// then I am returned all environments including "jamie"

// given the environment I'm using includes "ben"
// when I pass in "ben" as a flag
// then I am returned all environments including "ben"

// 